<h1>Yours article</h1>

	<h2><%= @artic.title %></h2>	
	<p><%= @artic.text %></p>
	<p><%= @artic.created_at %></p>

<%= form_for([@articl, @article.comments.build]) do |f| %>
	<p>
		<%= f.label :author %> <br>
		<%= f.text_field :author %>
	</p>

	<p>
		<%= f.label :body %> <br>
		<%= f.text_area :body %>
	</p>
	<p>
		<%= f.submit 'comment' %>
	</p>
<% end %> 